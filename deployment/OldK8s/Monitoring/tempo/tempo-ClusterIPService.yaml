apiVersion: v1
kind: Service
metadata:
  name: tempo
  namespace: job-scheduler
spec:
  selector:
    app: tempo
  ports:
    - name: http
      port: 3200
      targetPort: 3200
    - name: zipkin
      port: 9411
      targetPort: 9411
    # - name: otlp-grpc
    #   port: 4317
    #   targetPort: 4317
  type: ClusterIP