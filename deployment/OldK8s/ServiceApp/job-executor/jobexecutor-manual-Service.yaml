apiVersion: v1
kind: Service
metadata:
  name: job-manual-executor
  namespace: job-scheduler
spec:
  type: ClusterIP
  selector:
    app: job-manual-executor
  ports:
    - port: 8083
      targetPort: 8083
