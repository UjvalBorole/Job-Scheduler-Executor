apiVersion: v1
kind: Service
metadata:
  name: job-schedulersvc
  namespace: job-scheduler
spec:
  selector:
    app: job-schedulersvc
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: ClusterIP   # Internal service only
